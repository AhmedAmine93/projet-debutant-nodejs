/*const fonctionUn = (name) => {
    console.log(b + " " + `${name}`);
};*/
const fonctionDeux = (name) => {
    console.log(`le meilleur est: ${name}`);
    //console.log(b + " " + `${name}`);
};
/*const fonctionTrois = () => {
    console.log("le plus gros zboub est celui d'" + d);
};

const maFonction = function() {
    console.log(name.a);
};*/
const name = require('./les-variables');
console.log(name);


module.exports = fonctionDeux